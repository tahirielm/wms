{% extends 'base.html.twig' %}

{% block title 'Edit Product #' ~ product.id %}


{% block body %}

	<div class="row">
		<div class="col-md-6 mx-auto">

			<h1>
				Edit Product #{{ product.id }}</h1>

			{{ form_start(formEditProducts)}}

			{{ form_widget(formEditProducts)}}

			<input type="submit" value="Edit Product" class="btn btn-primary">
			{{ form_end(formEditProducts)}}

		</div>
	</div>


{% endblock %}
